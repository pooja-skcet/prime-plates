package com.example.demo.service;

import com.example.demo.entity.AdminAdd;

public interface AdminAddService {

    // Method to add a new menu item to the database
    AdminAdd addMenuItem(AdminAdd adminAdd);
}
